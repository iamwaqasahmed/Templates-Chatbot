# Shared Contracts

Shared types, schemas, and interfaces used across services.

This package will contain:

- Common Pydantic models (e.g., `ChatMessage`, `Conversation`)
- API contract types
- Event schemas for inter-service communication

> **Note:** This package is a placeholder. Shared contracts will be populated
> as the API and data models mature in later milestones.
